{
  "name": "blogg",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc && node dist/build.js",
    "watch": "tsc && node dist/build.js --watch",
    "serve": "tsc && node dist/serve.js",
    "dev": "npm run build && npm run serve",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "chokidar": "^4.0.0",
    "gh-pages": "^6.3.0",
    "marked": "^12.0.0",
    "typescript": "^5.0.0"
  }
}
